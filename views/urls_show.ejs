<!DOCTYPE html>
<html lang="en">
  <style>
    .flex-wrapper {
      display: flex;
      height: 100vh;
      flex-direction: column;
      justify-content: flex-start;
    }
  
    .footer {
      margin-top: auto;
    }

    .box-in {
      top: 50%;
      left: 50%;
      width: 40em;
      height: 22em;
      background-color: #01202405;
      box-shadow: 3px 3px 8px 1px #969696;
      border-radius: 25px;
      padding: 50px 0;
      text-align: center;
    }

    .center {
      margin: 0;
      position: absolute;
      top: 50%;
      left: 50%;
      -ms-transform: translate(-50%, -50%);
      transform: translate(-50%, -50%);
    }

  </style>
  <div class="flex-wrapper">
    <head>
      <!-- Bootstrap CSS -->
      <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
      <title>URL - TinyApp Example</title>
    </head>

    <body>
      <%- include('partials/_header') %>
      <% try { %>
        <% if (user) { %>
          <% if (urls.length === 0) { %>
            <br>
            <center><h5 style = "margin: 15%">You do not own this short URL.</h5></center>
          <% } else { %>
            <div class="d-flex h-100">
              <div class="m-auto">
                <div class="box-in">
                  <div class="center">
                    <h4>TinyURL for: <a href="<%= longURL %>"><%= longURL %></a></h4>
                    <h5>Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></h5>
                    <br>
                    <form class="form-inline" method="POST" action="/urls/<%=shortURL%>">
                      <label for="longURL">Edit the URL:</label>
                      <input class="form-control" type = "url" name = "longURL" placeholder="http://" style="width: 300px; margin-left: 1em">
                      <button type="submit" class="btn btn-primary" style="margin-left: 1em">Update</button>
                    </form>
                    <br>
                    <br>
                    <br>
                    <center>
                      <div style ='color:#28a745; width: 400px'>
                        Total TinyURL Visits = <%= visitCount %><br />Total Worldwide TinyURLs Created for this URL = <%= uniqueVisitCount %>
                      </div>
                    </center>
                  </div>
                </div>
              </div>
            </div>
        <% } } } catch (error) { %>
          <br>
          <center><h5 style = "margin: 15%">You need to <a href = "http://localhost:8080/register">create an account</a> or <a href = "http://localhost:8080/login">log in</a> to view URLs.</h5></center>
      <% } %>
            
      <!-- Bootstrap JS -->
      <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>

      <!-- Footer -->
        <div class = "footer">
          <%- include('partials/_footer') %>
        </div>
      </body>
</div>
</html>